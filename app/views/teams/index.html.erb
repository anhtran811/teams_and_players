<%= link_to 'Player Index', '/player_table_name'%>
<%= link_to 'Teams Index', '/teams'%>

<h1>Teams</h1>
<% @teams.each do |team| %>
  <p><%= team.name %> -- Created_at: <%= team.created_at %></p>
<% end %>
